describe('World', () => {

    it('should work', () => {
        expect(2).toBe(2);
    });

    it('should work as well', () => {
        expect(1).toBe(1);
    });

    it('should work as well', () => {
        expect(1).toBe(1);
    });

});
